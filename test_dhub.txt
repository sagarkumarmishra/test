Got it — here is **exact step-by-step (no confusion, just follow)** 👇

---

# ✅ STEP 1 — Open new notebook cell

Paste this:

```python
import requests
import urllib3

urllib3.disable_warnings(urllib3.exceptions.InsecureRequestWarning)

auth_url = "https://int-allianz-re.apis.allianz/edgemicro-auth-dhub/token"

print("Step 1: Getting token...")
```

---

# ✅ STEP 2 — Call token API

Paste below in same cell:

```python
response = requests.post(
    auth_url,
    auth=("###dh-hub-int--u###", "###dh-hub-int--p###"),
    verify=False
)

print("Status:", response.status_code)
print("Response:", response.text)
```

---

# ✅ STEP 3 — Extract token

Paste below:

```python
token = response.json().get("access_token")

print("Token value:")
print(token)
```

---

## 👉 STOP HERE and check

If you see:

```text
Status: 200
Token: eyJhbGciOi...
```

👉 GOOD ✅ → go next

---

If NOT → send me response

---

# ✅ STEP 4 — Create upload request

Open **new cell** and paste:

```python
import json
from datetime import datetime, timezone

url = "https://dhub-int.cta.allianz/api/v1/file/upload"

headers = {
    "Authorization": f"Bearer {token}",
    "Accept": "application/json",
    "tenant": "AZRE"
}

data = {
    "eventTime": datetime.now(timezone.utc).isoformat(),
    "policyId": "POL123",
    "status": "ACTIVE",
    "amount": 1000
}
```

---

# ✅ STEP 5 — Prepare file

```python
files = {
    "file": ("test.json", json.dumps(data), "application/json")
}
```

---

# ✅ STEP 6 — Send POST request

```python
print("Step 2: Uploading file...")

response = requests.post(
    url,
    headers=headers,
    files=files,
    verify=False
)

print("Status:", response.status_code)
print("Response:", response.text)
```

---

# ✅ FINAL RESULT EXPECTED

If everything correct:

```text
Status: 200
```

---

# 🚨 Common mistakes (avoid)

❌ Don’t do this:

```python
token = "https://..."
```

❌ Don’t skip Step 1

❌ Don’t forget:

```python
"Authorization": f"Bearer {token}"
```

---

# 🧠 Flow in one line

```text
Get token → Use token → Upload file
```

---

If it still fails, just send me:

* Step 2 response
* Step 6 status

I’ll fix it immediately.
