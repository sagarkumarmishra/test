**Yes, I understand your frustration.**  
You've tried everything correctly, but the token is **still not getting injected** from the Pipeline. This is a known annoying issue in some Synapse workspaces (especially with certain Spark pool configurations).

### ✅ Quick Temporary Solution: Pass the token **manually** for testing

Since the Web Activity is working fine and giving you the token, we can **hardcode the token temporarily** in the notebook just to test if the **upload part** works.

**Do this right now:**

**In your notebook, replace the token reading part with this:**

```python
import json
from datetime import datetime
import requests

print("=== DataHub Upload Started ===")

# === TEMPORARY: Paste the token from Web Activity here ===
# Copy the full "token" value from your Web Activity output
token = "eyJraWQiOiIxIiwidHlwIjoiSldUIiwiYWxnIjoiUlMyNTYifQ..g0tN1PEL9KzQXoze0KRDlDeZ9FJxOUFlueGv2rzEwSjMlw4OvC1v2ZQfqZoi1cTlLNB4q1IeQh5VeHfyREc2xK3VHk4leqWSIrpnqlkXfiAg9fCpYsfkVuZhhc2aIs7bZlqovB5kjkcp-7NfOu7oJIcBrzK1TWbfr4sQG-6eKYYqIyzIE66xCzp00EPIWRaFUyvev_kL6jpBx4jwRKayf8mhCXtqqlAppPDat92MKHtMdXy3MOtURfkVT1MipXWfRgXIBRxdr18RgxYiWtX3Mfb37Hc7eb4gpslxGjeA0NQTyrWNCpdPL09ceJgwGEtBYcjpzYRKEtykLifcw4CT2w"

print("Token length:", len(token))
print("Token preview:", token[:100] + "...")

# === Your data ===
data = {
    "eventTime": datetime.utcnow().strftime("%Y-%m-%dT%H:%M:%SZ"),
    "policyId": "POL123",
    "status": "ACTIVE",
    "amount": 1000
}

json_content = json.dumps(data)

print("Data prepared:", json_content)

# === Upload to DataHub ===
url = "https://dhub-int.cta.allianz/api/v1/file/upload"

headers = {
    "Authorization": f"Bearer {token}",
    "Accept": "application/json",
    "tenant": "AZRE"
}

files = {
    "file": ("test.json", json_content, "application/json")
}

print("Sending multipart upload to DataHub...")

response = requests.post(url, headers=headers, files=files, timeout=120)

print("Status Code:", response.status_code)
print("Response Body:", response.text)

if response.status_code == 200:
    print("✅ SUCCESS: Data uploaded to DataHub!")
else:
    print("❌ FAILED")
    raise Exception(f"Upload failed with status {response.status_code}: {response.text}")
```

### How to use it:

1. Copy the **full token** from your Web Activity output (the long string starting with `eyJraWQi...`).
2. Paste it inside the `token = "..."` line (replace the old one I put above).
3. **Save** and **Publish** the notebook.
4. Run the notebook directly (**Run all**) or from the Pipeline (Debug).

**This will bypass the parameter passing issue** and let us test if the **multipart upload** actually works.

---

### After you run it, please share:

- The full printed output (especially **Status Code** and **Response Body**)

Once we see whether you get **200 Success**, **401 Unauthorized**, **415 Unsupported Media Type**, or any other error, we can fix the final upload part.

Then we can go back and fix the proper token passing from the Pipeline.

**Try this manual token approach now** and paste the output here.  
This will tell us if the actual API call is working or not.

Let me know the result! 🚀