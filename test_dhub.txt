Perfect 👍 now everything is clear. You are on the **correct enterprise flow (Key Vault + Apigee OAuth)**.

I’ll give you **exact working steps for Synapse Spark notebook** — clean and simple.

---

# 🔥 WHAT YOU HAVE NOW

From DevOps:

* Key Vault: `KeyVault001`
* Secrets:

  * `dhub-api-client-id`
  * `dhub-api-client-secret`
  * `dhub-api-token-url`

👉 This means:

* You **DO NOT hardcode credentials**
* You fetch them using `mssparkutils`

---

# ✅ STEP-BY-STEP (FINAL WORKING FLOW)

---

## ✅ STEP 1 — Get secrets from Key Vault

```python
from notebookutils import mssparkutils

print("Fetching secrets from Key Vault...")

client_id = mssparkutils.credentials.getSecret("KeyVault001", "dhub-api-client-id")
client_secret = mssparkutils.credentials.getSecret("KeyVault001", "dhub-api-client-secret")
token_url = mssparkutils.credentials.getSecret("KeyVault001", "dhub-api-token-url")

print("Secrets fetched successfully")
```

---

## ✅ STEP 2 — Generate token (Apigee)

```python
import requests
import urllib3

urllib3.disable_warnings(urllib3.exceptions.InsecureRequestWarning)

print("Getting token from Apigee...")

payload = {
    "grant_type": "client_credentials",
    "client_id": client_id,
    "client_secret": client_secret
}

headers = {
    "Content-Type": "application/x-www-form-urlencoded"
}

response = requests.post(
    token_url,
    data=payload,
    headers=headers,
    verify=False
)

print("Status:", response.status_code)

token = response.json().get("access_token")

print("Token received")
```

---

## ⚠️ STOP & CHECK

If:

```text
Status: 200
```

👉 Good → continue

If not → send me response

---

## ✅ STEP 3 — Upload file to DataHub

```python
import json
from datetime import datetime, timezone

url = "https://dhub-int.cta.allianz/api/v1/file/upload"

headers = {
    "Authorization": f"Bearer {token}",
    "Accept": "application/json",
    "tenant": "AZRE"
}

data = {
    "eventTime": datetime.now(timezone.utc).isoformat(),
    "policyId": "POL123",
    "status": "ACTIVE",
    "amount": 1000
}

files = {
    "file": ("test.json", json.dumps(data), "application/json")
}

print("Uploading file to DataHub...")

response = requests.post(
    url,
    headers=headers,
    files=files,
    verify=False
)

print("Status:", response.status_code)
print("Response:", response.text)
```

---

# ✅ EXPECTED RESULT

```text
Status: 200
```

---

# 🧠 FULL FLOW (UNDERSTAND THIS)

```text
KeyVault → Get client_id & secret
        ↓
Apigee Token API → Generate token
        ↓
Use token → Call DataHub API
        ↓
Upload file ✅
```

---

# 🚨 COMMON ISSUES

### ❌ 401

→ Token issue (wrong / expired)

### ❌ 404

→ Wrong token_url

### ❌ 415

→ Wrong content type (already fixed in your case)

---

# 🧾 WHAT YOU CAN TELL YOUR TEAM

> I am now using Key Vault to fetch Apigee credentials and generating token via client_credentials flow. Proceeding with authenticated DataHub API call.

---

# 👍 YOU ARE VERY CLOSE

You’ve already solved:

* Network ✅
* Endpoint ✅
* Upload logic ✅

Now just:

```text
Token → Attach → Done
```

---

If anything fails, just paste:

* Token API response
* Upload response

I’ll fix instantly 👍
