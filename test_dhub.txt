Got it — here is the **final clean step-by-step (based on your exact setup)**. Just follow line by line 👇

---

# 🎯 YOUR SETUP (CONFIRMED)

Your Web Activity returns:

```json
{
  "token": "xxxxx",
  "token_type": "Bearer"
}
```

👉 So correct path is:

```text
.output.token
```

---

# ✅ STEP 1 — Fix Pipeline Parameter

Go to:

👉 **Pipeline → Notebook activity → Settings → Base parameters**

### ❗ Remove old param (`bearer_token`)

### ➕ Add NEW:

| Name         | Value                                    |
| ------------ | ---------------------------------------- |
| access_token | @activity('GetApigeeToken').output.token |

---

# ✅ STEP 2 — Save pipeline

Click **Publish / Save**

---

# ✅ STEP 3 — Read token in Notebook

Open notebook → add this **FIRST CELL**

```python
from notebookutils import mssparkutils

print("Reading token...")

token = mssparkutils.notebook.getParameter("access_token")

print("Token received:", token[:20])
```

---

# ▶️ STEP 4 — Test token only

Run pipeline → **Debug**

### Expected:

```text
Reading token...
Token received: eyJhbGciOi...
```

---

## ❗ If token is None

→ parameter name mismatch → recheck Step 1

---

# ✅ STEP 5 — Prepare data

Add next cell:

```python
import json
from datetime import datetime, timezone

data = {
    "eventTime": datetime.now(timezone.utc).isoformat(),
    "policyId": "POL123",
    "status": "ACTIVE",
    "amount": 1000
}

print("Data ready")
```

---

# ✅ STEP 6 — Send to DataHub

Add next cell:

```python
import requests
import urllib3

urllib3.disable_warnings(urllib3.exceptions.InsecureRequestWarning)

url = "https://dhub-int.cta.allianz/api/v1/file/upload"

headers = {
    "Authorization": f"Bearer {token}",
    "Accept": "application/json",
    "tenant": "AZRE"
}

files = {
    "file": ("test.json", json.dumps(data), "application/json")
}

print("Sending data...")

response = requests.post(
    url,
    headers=headers,
    files=files,
    verify=False
)

print("Status:", response.status_code)
print("Response:", response.text)
```

---

# ▶️ STEP 7 — Run full pipeline

Click **Debug**

---

# ✅ FINAL SUCCESS

```text
Status: 200
```

---

# 🚨 If error comes

### ❌ 401

→ Token not passed correctly

### ❌ 403

→ Access issue (team side)

### ❌ 415

→ Content-type issue (already solved before)

---

# 🧠 FINAL FLOW (REMEMBER)

```text
Web Activity → token
        ↓
Pipeline → pass token
        ↓
Notebook → read token
        ↓
Notebook → send file
        ↓
DataHub success ✅
```

---

# 👍 You are DONE after this

This is exactly how **real enterprise pipelines work**.

---

If anything fails, just send:

* Notebook output
* Pipeline output

I’ll fix instantly 👍
