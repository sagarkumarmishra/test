**Great!** The **Web Activity (GetApigeeToken)** is working perfectly.

Your token output looks correct:
- It returns a valid JWT token (starts with `eyJraWQi...`)
- `token_type`: "bearer"
- Status 200
- The token is quite long and has a good expiry (`expires_in`: 7200000 seconds = 2 hours)

The **problem is only in passing this token** from the Web Activity → Notebook Activity.

### Why the notebook is getting killed every time
The notebook code is failing early (most likely on the token line) → it raises an exception → Synapse kills the Spark session (that's why you see `state: "killed"`, `result: "cancelled"` even though the overall pipeline says "Succeeded").

### Final Recommended Fix (Clean & Most Reliable)

**Step 1: Clear your notebook and use this exact setup**

**Cell 1 (First cell – Parameters cell – keep it simple)**

```python
# === PARAMETERS CELL ===
# Toggle this cell as "Parameters cell" using the ... menu
bearer_token = ""
```

→ Click **...** on this cell → **Toggle parameter cell** (you should see "Parameters" label).

**Cell 2 (Main code – put this below it)**

```python
import json
from datetime import datetime
import requests

print("=== DataHub Upload Started ===")

# Get token injected by Pipeline (Parameters cell method)
token = bearer_token.strip() if bearer_token else ""

print("Token length received:", len(token))
print("Token preview:", token[:80] + "..." if len(token) > 80 else "EMPTY")

if len(token) < 100:
    raise Exception("❌ Token not received from Pipeline.\n"
                    "Check: Base parameter Name must be exactly 'bearer_token'\n"
                    "Value must be @activity('GetApigeeToken').output.token")

print("✅ Token looks good!")

# Prepare your JSON data
data = {
    "eventTime": datetime.utcnow().strftime("%Y-%m-%dT%H:%M:%SZ"),
    "policyId": "POL123",
    "status": "ACTIVE",
    "amount": 1000
}

json_content = json.dumps(data)

print("Data prepared:", json_content)

# === MULTIPART UPLOAD ===
url = "https://dhub-int.cta.allianz/api/v1/file/upload"

headers = {
    "Authorization": f"Bearer {token}",
    "Accept": "application/json",
    "tenant": "AZRE"
}

files = {
    "file": ("test.json", json_content, "application/json")
}

print("Sending request to DataHub...")

response = requests.post(url, headers=headers, files=files, timeout=120)

print("Status Code:", response.status_code)
print("Response:", response.text)

if response.status_code == 200:
    print("✅ SUCCESS: Data uploaded to DataHub!")
else:
    raise Exception(f"Failed with status {response.status_code}: {response.text}")
```

### What you must double-check in the Pipeline

In **Notebook Activity → Settings → Base parameters**:

- **Name**: `bearer_token`   ← **exactly** this (lowercase b, no extra space)
- **Type**: String
- **Value**: `@activity('GetApigeeToken').output.token`   ← **Important**: Use `.output.token` (not `.output.body.token`)

**Save** the notebook → **Publish** it.

Then run **Debug** on the full pipeline.

After it finishes (even if killed), open the **Notebook output** and paste here **only the printed lines** (Token length, Token preview, Status Code, etc.).

### Alternative (if Parameters cell still doesn't work)

If token is still empty after the above, replace Cell 1 & 2 with this version (no toggle needed):

**Cell 1:**
```python
from notebookutils import mssparkutils
bearer_token = mssparkutils.notebookutils.getArgument("bearer_token", "")
```

Then use the same Cell 2 as above (just change the token line to `token = bearer_token.strip()`).

Try the **Parameters cell** version first (it's cleaner when it works).

Run the pipeline with the above and share the **printed output** from the notebook.  
We'll see immediately if the token arrived or not, and fix the next error (401/415/etc.).

You're very close — the token generation is solid. Just the passing part needs to click. Let me know the result! 🚀