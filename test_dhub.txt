**Got it!** The error is the same one we've seen before: `module 'notebookutils.mssparkutils.notebook' has no attribute 'params'`.

This happens because the exact path `mssparkutils.notebook.params.get()` is **not available** in your current Synapse Spark runtime/environment.

### Working Method (Most Reliable for Pipeline → Notebook)

Use **`mssparkutils.notebookutils.getArgument()`** — this is the method that consistently works across many Synapse setups when pipeline parameters are passed.

**Replace your current first cell with this exact code:**

```python
# === GET TOKEN FROM PIPELINE - WORKING METHOD ===
from notebookutils import mssparkutils

# Get the parameter passed from the Pipeline Notebook Activity
bearer_token = mssparkutils.notebookutils.getArgument("bearer_token", "")

print("Token length received:", len(bearer_token))
print("Token preview (first 100 chars):", bearer_token[:100] + "..." if len(bearer_token) > 100 else bearer_token)
```

**Then keep your second cell (the upload code) as it is**, but update the token line inside it to:

```python
token = bearer_token.strip() if bearer_token else ""
```

### Full Recommended Notebook (Two Cells)

**Cell 1 (Top):**

```python
# === GET TOKEN FROM PIPELINE ===
from notebookutils import mssparkutils

bearer_token = mssparkutils.notebookutils.getArgument("bearer_token", "")
print("=== Token from Pipeline ===")
print("Length:", len(bearer_token))
print("Preview:", bearer_token[:100] + "..." if len(bearer_token) > 100 else "EMPTY")
```

**Cell 2 (Main Upload):**

```python
import json
from datetime import datetime
import requests

print("=== DataHub Upload Started ===")

token = bearer_token.strip() if bearer_token else ""

if len(token) < 100:
    raise Exception("❌ Token is still empty. Make sure Base parameter Name = 'bearer_token' exactly and run via Pipeline Debug.")

print("✅ Token received! Length:", len(token))

# Prepare data
data = {
    "eventTime": datetime.utcnow().strftime("%Y-%m-%dT%H:%M:%SZ"),
    "policyId": "POL123",
    "status": "ACTIVE",
    "amount": 1000
}

json_content = json.dumps(data)

print("Data prepared:", json_content)

# Upload
url = "https://dhub-int.cta.allianz/api/v1/file/upload"

headers = {
    "Authorization": f"Bearer {token}",
    "Accept": "application/json",
    "tenant": "AZRE"
}

files = {
    "file": ("test.json", json_content, "application/json")
}

print("Sending request...")

response = requests.post(url, headers=headers, files=files, timeout=120)

print("Status Code:", response.status_code)
print("Response:", response.text)

if response.status_code == 200:
    print("✅ SUCCESS: Uploaded to DataHub!")
else:
    raise Exception(f"Failed: {response.status_code} - {response.text}")
```

### Next Actions

1. Replace the first cell with the code above.
2. **Save** the notebook.
3. **Publish** the notebook (important!).
4. Go to your **Pipeline** → Notebook Activity → confirm:
   - Base parameters → **Name**: `bearer_token` (exactly)
   - **Value**: `@activity('GetApigeeToken').output.token`
5. Run **Debug** on the full pipeline.
6. After it finishes, open the **Notebook output** and copy-paste **all the printed lines** (especially the token length and preview).

If the token length is now **~700+ characters**, we are good — the next error will likely be 401 (token rejection) or 200 (success), and we can fix it quickly.

If it's still 0/EMPTY, please also share a **screenshot** of your **Notebook Activity → Base parameters** section.

Try this and reply with the new notebook printed output. This should finally get the token through! 🚀