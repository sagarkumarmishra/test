Perfect 👌 — this is exactly what you needed.
They’ve now given you the **actual token endpoint**.

---

# What this means

You should call this URL to get token:

```id="p3j6y8"
https://int-allianz-re.apis.allianz/edgemicro-auth-dhub/token
```

👉 This is your **Apigee auth endpoint**

---

# Now follow this exact working flow

## Step 1 — Get token (IMPORTANT)

Try this code **as it is**:

```python
import requests
import urllib3

urllib3.disable_warnings(urllib3.exceptions.InsecureRequestWarning)

auth_url = "https://int-allianz-re.apis.allianz/edgemicro-auth-dhub/token"

# try basic auth format first
response = requests.post(
    auth_url,
    auth=("###dh-hub-int--u###", "###dh-hub-int--p###"),
    verify=False
)

print("Status:", response.status_code)
print("Response:", response.text)
```

---

## What you expect

If correct, you’ll get:

```id="5br9sk"
Status: 200
{"access_token":"xxxxx"}
```

---

## If this DOES NOT work

Then try this version (some Apigee setups need form data):

```python
response = requests.post(
    auth_url,
    data={
        "username": "###dh-hub-int--u###",
        "password": "###dh-hub-int--p###"
    },
    verify=False
)
```

---

# Step 2 — Use token for upload

Once you get token:

```python
token = "PASTE_TOKEN_HERE"
```

Then run:

```python
import json
from datetime import datetime, timezone

url = "https://dhub-int.cta.allianz/api/v1/file/upload"

headers = {
    "Authorization": f"Bearer {token}",
    "Accept": "application/json",
    "tenant": "AZRE"
}

data = {
    "eventTime": datetime.now(timezone.utc).isoformat(),
    "policyId": "POL123",
    "status": "ACTIVE",
    "amount": 1000
}

files = {
    "file": ("test.json", json.dumps(data), "application/json")
}

response = requests.post(
    url,
    headers=headers,
    files=files,
    verify=False
)

print("Status:", response.status_code)
print(response.text)
```

---

# Very important notes ⚠️

* If you still get **401 → token issue**
* If you get **403 → permission issue**
* If you get **200 → DONE ✅**

---

# What you can tell your team now

> I am able to reach DataHub and now trying authentication via Apigee token endpoint. Testing token generation using provided credentials.

---

# Where you are now

```id="sl3noq"
Connection ✅
Endpoint ✅
Auth endpoint ✅
→ Token (in progress)
→ Upload (next step)
```

---

If you want, send me the **token API response**, I’ll fix it instantly (no trial & error).
