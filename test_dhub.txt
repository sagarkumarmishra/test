**I see the pattern — the token is still not reaching the notebook.**

This is a **very common** issue in Azure Synapse. The **Parameters cell + toggle** method is the official way, but sometimes it doesn't inject properly if:
- The cell is not the **very first** cell
- The toggle is not applied correctly
- The notebook wasn't **Published** after changes
- The **Base parameter Name** in the Pipeline has a small typo (space, capital letter, etc.)

### Let's do a **clean reset** and try again (step-by-step)

**1. Clean your notebook completely**

- Delete **all cells** in your notebook (or create a new notebook to avoid old issues).
- Add **exactly these two cells**:

**Cell 1 (Must be the top-most cell):**

```python
# === PARAMETERS CELL - DO NOT MOVE THIS CELL ===
# Click the ... on this cell → "Toggle parameter cell"
# You MUST see the word "Parameters" on this cell after toggling

bearer_token = ""   # Pipeline will replace this empty string with the real token
```

- After pasting, click the **...** (ellipses) on the right of this cell → select **"Toggle parameter cell"**.
- Confirm the cell now shows **"Parameters"** label.

**Cell 2 (Right below it):**

```python
import json
from datetime import datetime
import requests

print("=== DataHub Upload Started ===")

# Token injected by Pipeline
token = bearer_token.strip() if bearer_token else ""

print("Token length:", len(token))
print("Token preview (first 120 chars):", token[:120] + "..." if len(token) > 120 else "EMPTY")

if len(token) < 100:
    raise Exception("Token is EMPTY. Please check the 5 points below.")

print("✅ Token received! Proceeding with upload...")

# Your data
data = {
    "eventTime": datetime.utcnow().strftime("%Y-%m-%dT%H:%M:%SZ"),
    "policyId": "POL123",
    "status": "ACTIVE",
    "amount": 1000
}

json_content = json.dumps(data)

# Upload
url = "https://dhub-int.cta.allianz/api/v1/file/upload"

headers = {
    "Authorization": f"Bearer {token}",
    "Accept": "application/json",
    "tenant": "AZRE"
}

files = {
    "file": ("test.json", json_content, "application/json")
}

print("Sending request to DataHub...")

response = requests.post(url, headers=headers, files=files, timeout=120)

print("Status Code:", response.status_code)
print("Response:", response.text)

if response.status_code == 200:
    print("✅ SUCCESS: Data uploaded to DataHub!")
else:
    print("❌ FAILED")
    raise Exception(f"Status {response.status_code}: {response.text}")
```

**2. Save + Publish**

- Click **Save**
- Click **Publish all** (or Publish)

**3. Check Pipeline Notebook Activity (Very Important)**

Go to your Pipeline → click on **Notebook1** activity → **Settings** tab → **Base parameters**

Make sure it looks **exactly** like this:
- **Name**: `bearer_token`   (lowercase `b`, no extra spaces, no quotes)
- **Type**: String
- **Value**: `@activity('GetApigeeToken').output.token`

**4. Run the Pipeline**

- Click **Debug** on the Pipeline (not "Run all" in the notebook).
- Wait for it to finish.
- Open the **Notebook output** and copy-paste **everything that is printed** (especially "Token length" and "Token preview").

### If token is still EMPTY after this

Please reply with:
1. The full notebook printed output
2. Screenshot of the **Base parameters** section in the Notebook Activity (Name + Value visible)
3. Confirm that the first cell shows the **"Parameters"** label

We will then try an alternative approach (using a different injection method or even moving the token fetch inside the notebook using Key Vault if allowed).

The Web Activity token is perfect — we just need the hand-off to the notebook to work.

Go ahead and set it up as above, then share the new output. We'll get it working.